interface:
  display_name: "Inventory Wms App"
  short_description: "Repo-specific patterns and workflows for inventory-wms-app"
  default_prompt: "Use the inventory-wms-app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true