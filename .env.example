# Firebase Configuration (these are public and safe to expose)
# Copy this file to .env.local and fill in your actual values

# ============================================================
# SECURITY: USE CLOUD FUNCTIONS IN PRODUCTION
# ============================================================
# Set to "true" to use secure Cloud Functions for UPC lookups
# Set to "false" to use direct API calls (development only)
# IMPORTANT: When true, API keys should be set in Cloud Functions environment,
# not here. See functions/.env.example
VITE_USE_CLOUD_FUNCTIONS=false

# ============================================================
# DEVELOPMENT ONLY - API Keys (NOT for production)
# ============================================================
# These keys are only used when VITE_USE_CLOUD_FUNCTIONS=false
# WARNING: These keys will be visible in your built JavaScript bundle!

# Go-UPC API Key (get from https://go-upc.com)
VITE_GOUPC_API_KEY=your-go-upc-api-key-here
# Alternative name (backward compatibility)
# VITE_GO_UPC_KEY=your-go-upc-api-key-here

# UPCItemDB API Key (get from https://devs.upcitemdb.com)
# Only needed if using the authenticated endpoint instead of trial
VITE_UPCITEMDB_KEY=your-upcitemdb-api-key-here

# ============================================================
# USER AUTHENTICATION
# ============================================================
# Allowed Email Domain (restrict user registration to specific domain)
# Leave empty to allow any email domain
VITE_ALLOWED_EMAIL_DOMAIN=yourdomain.com
