# Cloud Functions Environment Variables
# These are kept secure on the server and never exposed to clients

# Go-UPC API Key (get from https://go-upc.com)
# This is the preferred UPC lookup service
GO_UPC_API_KEY=your-go-upc-api-key-here

# UPCItemDB API Key (get from https://devs.upcitemdb.com)
# Optional - if not provided, uses trial endpoint with limited data
UPCITEMDB_API_KEY=your-upcitemdb-api-key-here

# ============================================================
# HOW TO SET THESE IN FIREBASE:
# ============================================================
#
# For local testing with emulator:
# 1. Copy this file to .env
# 2. Fill in your actual API keys
# 3. Run: firebase emulators:start
#
# For production deployment:
# Use Firebase CLI to set environment variables:
#
# firebase functions:secrets:set GO_UPC_API_KEY
# firebase functions:secrets:set UPCITEMDB_API_KEY
#
# Or use the newer dotenv approach:
# 1. Create .env file (don't commit!)
# 2. Deploy with: firebase deploy --only functions
#
# To view current config:
# firebase functions:config:get
#
# ============================================================
